function upperCaseWords(stringInput)
{
	return stringInput.toUpperCase();

}
module.exports = upperCaseWords;

  

// another way of writing it:

   // module.exports = function(input) {
   //      return input.toUpperCase()
   //    }
